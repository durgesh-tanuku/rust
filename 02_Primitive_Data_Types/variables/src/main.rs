fn main() {
    /* let is used to declare a variable. By default the variable is immutable
       mut is used to make the variable mutable (to re-assign the value of variable) */
    let mut x = 10;
    println!("x is {}", x);
    x = 20;
    println!("x is {}", x);
}
